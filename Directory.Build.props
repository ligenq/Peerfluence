<Project>
  <PropertyGroup>
    <!-- Kept in one place so the projects do not drift to different package versions. -->
    <PeerSharpPackageVersion>3.1.0</PeerSharpPackageVersion>
  </PropertyGroup>
</Project>
